"""Standalone diagnostic helper (NOT a conftest — kept outside pytest discovery).

This file is imported by scripts/c12_bisect_early_stops.py — not
collected by pytest (name doesn't match test_ prefix and it's under
tests/taaqol_integration/ but pytest ignores non-test_ files).
"""
